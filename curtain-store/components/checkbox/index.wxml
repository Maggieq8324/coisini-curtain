<!-- 购物车Item 勾选组件 -->
<view bind:tap="onCheck" class="container {{checked?'bg-checked':'bg-unchecked'}}">
    <l-icon wx:if="{{checked?true:false}}" name="check" size="28" l-class="curtain"></l-icon>
</view>